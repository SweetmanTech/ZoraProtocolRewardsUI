import SkeletonCard from "../SkeletonCard"

const LoadingPage = () => (
  <div className="flex items-center justify-center w-full h-screen bg-none">
    <SkeletonCard />
  </div>
)

export default LoadingPage
